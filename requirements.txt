pydicom>=2.4.0
numpy>=1.24.0
torch>=2.1.0
torchvision>=0.16.0
tqdm>=4.65.0
matplotlib>=3.7.0
scipy>=1.11.0

# Visualization (visualize_output.ipynb, axial_view.AxialView.interactive)
jupyter>=1.0.0
ipywidgets>=8.0.0

# Cross-timepoint ROI propagation (4_propagate_roi.py)
SimpleITK>=2.3.0

# Feature extraction (6_extract_features.py) — Otsu / multi-Otsu thresholds.
# pyradiomics is deliberately NOT used: it does not install on current
# Python/NumPy; the IBSI-style features are implemented on numpy/scipy/skimage.
scikit-image>=0.22.0
